
import React from 'react';

function SearchBar() {
    return (
        <div>
            <input type="text" placeholder="Rechercher" />
            {/* Ajoutez ici les éléments de filtrage */}
        </div>
    );
}

export default SearchBar;
